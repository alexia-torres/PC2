<h1>{{title}}</h1>

<h1> Productos</h1>
    <p >
        <div class="container">
            <h3><a href="/usuarios/create">Añadir Producto</a></h3>
            <h1>Lista de Productos</h1>
            {{#if usuarios }}
                <table class="table table-hover">
                    <thead>
                        <tr>
                            <th>Codigo</th>
                            <th>Descripcion</th>
                            <th>Precio</th>
                            <th>Stock</th>
                            <th>Marca</th>
                        </tr>
                    </thead>
                    <tbody>
                        {{# each usuarios }}
                        <tr>
                            <td><a href="/usuarios/show/{{ _id }}">{{ codigo }}</a></td>
                            <td>{{ descripcion }}</td>
                            <td>{{ precio }}</td>
                            <td>{{ stock }}</td>
                            <td>{{ marca }}</td>
                        </tr>
                        {{/each}}
                    </tbody>
                </table>
            {{~^~}}
                <div>Productos no encontrados.</div>
            {{/if}}
        </div>
    </p>
